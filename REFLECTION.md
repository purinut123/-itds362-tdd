# บทสรุปทบทวน (Retrospective)

**1. มีเทสต์ทั้งหมดกี่ตัวและโค้ดจริงกี่บรรทัด อัตราส่วนเป็นเท่าใด?**
> **ตอบ:** มีเทสต์ทั้งหมด `[6]` ตัว และมีโค้ดจริงในไฟล์ `kitchen.py` จำนวน `[22]` บรรทัด คิดเป็นอัตราส่วน เทสต์ต่อโค้ดจริงคือ `[6:22]`

---

**2. ช่วงที่แถบเป็นสีแดงนานที่สุดคือเมื่อใด อะไรทำให้มันนาน?**
> **ตอบ:** ช่วงที่ใช้เวลานานที่สุดคือขั้นตอน `[A1 -> A3]` สาเหตุที่ทำให้ใช้เวลานานเพราะ `[ในการปรับโครงสร้างโค้ด (Refactor) ในขั้นตอน A3 ที่ต้องเปลี่ยนออบเจกต์ให้เป็นแบบ Value Object คือการคืนค่าออบเจกต์ตัวใหม่แทนการแก้ไขค่าเดิม ทำให้เทสต์เก่าของ A1 และ A2 รันไม่ผ่าน]`

---

**3. หาจุดที่ใช้ Fake It หนึ่งจุด ใช้ Triangulate หนึ่งจุด และใช้ Obvious Implementation หนึ่งจุด เมื่อมองย้อนกลับไปแต่ละครั้งเลือกได้เหมาะสมหรือไม่?**
> **ตอบ:**
> *   **Fake It:** ใช้ตอนที่ `[ขั้นตอน A1 คืนค่า 600 ตรงๆ]` รู้สึกว่าเหมาะสมเพราะ `[ช่วยลดความซับซ้อนในก้าวแรก ทำให้เราได้ทดสอบและมั่นใจก่อนว่าระบบเทสต์ โครงสร้างไฟล์ และคลาส Quantity สามารถเชื่อมต่อกันได้จริง ก่อนที่จะเสียเวลาไปคิดตรรกะการคำนวณ]`
> *   **Triangulate:** ใช้ตอนที่ `[ขั้นตอน A2 เพิ่มเทสต์การคูณด้วย 2]` รู้สึกว่าเหมาะสมเพราะ `[การมีเทสต์ 2 เคสที่ผลลัพธ์ต่างกัน เป็นการบังคับให้เราไม่สามารถใช้ค่าคงที่หลอกๆ ได้อีกต่อไป และถูกผลักดันให้ต้องเขียนสูตรการคูณที่แท้จริง (self.amount * multiplier) เพื่อให้เทสต์ผ่านทั้งคู่]`
> *   **Obvious Implementation:** ใช้ตอนที่ `[การเขียน __eq__ หรือเขียนตรรกะบวกเลขใน Sum]` รู้สึกว่า `[เราเห็นภาพชัดเจนแล้วว่าการบวกคือการเอา amount ของตัวซ้ายและขวามาบวกกัน ตรรกะมีความตรงไปตรงมา สั้น จึงพิมพ์โค้ดจริงลงไปได้เลยโดยไม่ต้องทำ Fake It ก่อน]`

---

**4. ยังมีรายการใดค้างอยู่ในรายการทดสอบ รายการใดที่จะลงมือทำจริง และรายการใดที่ควรลบทิ้งเพราะเป็นการคาดเดาล่วงหน้า?**
> **ตอบ:**
> *   **รายการที่ค้างอยู่:** `[ระบุรายการจากด้านบนของไฟล์ test_kitchen.py ที่ยังไม่ได้ขีดฆ่า เช่น (200 g + 1 oz) × 2]`
> *   **รายการที่จะลงมือทำจริง:** `[การแปลงหน่วยจริงๆ ใน Converter]`
> *   **รายการที่ควรลบทิ้ง (คาดเดาล่วงหน้า):** `[การกำหนด __hash__ เพราะตอนนี้เรายังไม่มี Requirement ที่ต้องใช้ Quantity เป็น Key ใน Dictionary]`

---

**5. ดูเมท็อด Quantity.reduce ถ้าออกแบบระบบทั้งหมดบนกระดาษก่อนนักศึกษาจะเขียนเมท็อดนี้ออกมาหรือไม่?**
> **ตอบ:** `[ถ้าให้ออกแบบระบบเองบนกระดาษตั้งแต่แรก คิดว่าตัวเองคงคิดไม่ถึงและไม่ได้ออกแบบเมท็อด reduce ออกมาแบบนี้ครับ เพราะปกติตอนเขียนโค้ด ก็คงพยายามเขียนตรรกะในฟังก์ชัน plus ให้มันแปลงหน่วยและบวกเลขให้เสร็จจบไปเลยทีเดียวครัย]`